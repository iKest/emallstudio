import BaseScene from "../scene";

/**
 * Класс сцены победы
 * @extends {Scene}
 */
export default class WinScene extends BaseScene {

  /**
   * description
   */
  constructor() {
    super({
      key: "WinScene"
    });
  }

  /**
   * description
   */
  create() {
    super.create();
  }
}
